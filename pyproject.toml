[project]
name = "nbt-to-multiverse"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "nbt>=1.5.1",
]

[project.scripts]
convert = "convert:cli"
